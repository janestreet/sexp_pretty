(library
 ((name sexp_pretty)
  (public_name sexp_pretty)
  (preprocess (pps (ppx_base ppx_driver.runner)))
  (flags (:include ${ROOT}/ocamlflags))
  (libraries (base sexplib re.str))))


(jbuild_version 1)
